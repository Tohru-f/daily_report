## 取り組んだ課題一覧
- 【DB操作の革命】Prisma完全攻略
## わかったこと
- seed.tsで使用する数字に関数条件として、gt(greater than)やgte(greater than equall)がある。これは条件として設定される数字を含むかどうかを区別している。
- データベースにおけるselectメソッドは取得したデータベースの値をwhereなどで絞った後に必要なカラムだけをbooelanで指定することで選択取得ができる。
- 純粋に関連レコードを取得する場合、selectメソッドではなく、includeメソッドを使い、その中にselectメソッドを使えば関連レコードの必要なカラムだけを取得できる。
- incrementメソッドを使うと、レコード内の値を指定した数だけ増やせる。
- schema.prismaで値にインデックスを貼りたい時は`@@index`を使用することで検索が高速化する
## 次やること
- 【DB操作の革命】Prisma完全攻略
## 感じたこと
- データベースの設定は終わったので、API作成に移る
## 学習時間
- 2h 55min